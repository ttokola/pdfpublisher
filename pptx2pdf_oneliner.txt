$iniPath = "settings.ini"; if (-not (Test-Path $iniPath)) {; Write-Error "settings.ini not found at: $iniPath. Aborting script."; return; }; $lines = Get-Content $iniPath; $keys = @("lecture_slides_dir", "course_slides_dir"); $folders = foreach ($line in $lines) {; foreach ($key in $keys) {; if ($line -match "^\s*$key\s*=\s*(.+)$") {; $matches[1].Trim(); }; }; }; $folders = $folders | Where-Object { $_ -and $_.Trim() -ne "" }; if ($folders.Count -eq 0) {; Write-Error "No valid folder paths found in settings.ini. Please add course_slides_dir and lecture_slides_dir keys for directories to monitor. Aborting script."; return; }; Write-Host "Monitoring the following folders:"; $folders; $sleepSeconds = 300; Write-Host "Starting PPTX → PDF monitor..."; while ($true) {; Write-Host "Scanning folders at $(Get-Date)..."; $ppApp = $null; $powerpointWasRunning = $true; try {; $ppApp = [Runtime.InteropServices.Marshal]::GetActiveObject("PowerPoint.Application"); Write-Host "Powerpoint is running!"; } catch {; Write-Host "Powerpoint needs to be started."; $powerpointWasRunning = $false; $ppApp = New-Object -ComObject PowerPoint.Application; }; if (-not $powerpointWasRunning) {; try {; Visible = [Microsoft.Office.Core.MsoTriState]::msoFalse; } catch {; Write-Warning "PowerPoint cannot be hidden on this system. Continuing with visible window."; $ppApp.Visible = [Microsoft.Office.Core.MsoTriState]::msoTrue; }; }; foreach ($folder in $folders) {; if (-not (Test-Path $folder)) {; Write-Warning "Folder not found: $folder"; continue; }; Write-Host "Scanning folder $folder..."; $pptxFiles = Get-ChildItem -Path $folder -Filter *.pptx -File; foreach ($pptx in $pptxFiles) {; $pdf = [System.IO.Path]::ChangeExtension($pptx.FullName, ".pdf"); $needsUpdate = $false; if (-not (Test-Path $pdf)) {; Write-Host "PDF cannot be found for... $($pptx.Name)"; $needsUpdate = $true; } else {; $pptTime = (Get-Item $pptx.FullName).LastWriteTime; $pdfTime = (Get-Item $pdf).LastWriteTime; if ($pptTime -gt $pdfTime) {; Write-Host "PDF version is outdated for... $($pptx.Name)"; $needsUpdate = $true; }; }; if ($needsUpdate) {; Write-Host "Updating..."; try {; $presentation = $ppApp.Presentations.Open($pptx.FullName, $false, $false, $false); $presentation.SaveAs($pdf, 32); $presentation.Close(); } catch {; Write-Error "Failed to convert $($pptx.FullName): $_"; }; }; }; }; if (-not $powerpointWasRunning) {; Write-Host "Powerpoint was not running, closing..."; $ppApp.Quit(); }; if ((Get-Date).DayOfWeek -in 'Saturday','Sunday') {; $sleepSeconds = 21600; Write-Host "It's the weekend, sleeping for $($sleepSeconds/3600) hours..."; } else {; $hour = (Get-Date).Hour; if ($hour -ge 8 -and $hour -lt 15) {; $sleepSeconds = 300; Write-Host "Office hours, sleeping for $($sleepSeconds/60) minutes..."; } elseif ($hour -ge 15 -and $hour -lt 21) {; $sleepSeconds = 1800; Write-Host "Outside office hours, sleeping for $($sleepSeconds/60) minutes..."; } else {; $sleepSeconds = 10800; Write-Host "Night time... sleeping for $($sleepSeconds/3600) hours..."; }}; Start-Sleep -Seconds $sleepSeconds; }; 
